export class Exp {
    id?: number;
    nombreExp: string;
    descExp: string;

    constructor(nombreExp: string, descExp: string) {
        this.nombreExp = nombreExp;
        this.descExp = descExp;
    }
}
